Array - это набор переменных одного типа
		в непрерывной области пам€ти.



			type name[SIZE];
type - тип элементов массива.
	   ¬се элементы массива могут быть одного типа.
name - им€ массива. ƒл€ именовани€ массивов используютс€
	   такие же идентификаторы, как и дл€ именовани€ переменных.
SIZE - количество элементов массива.
	   ћожет быть задано лишь целочисленным константным значением.

			const int n = 5;
			int arr[n];